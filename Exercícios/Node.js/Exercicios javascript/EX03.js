// Template Strings:
// Crie três variáveis (número, string, boolean) e utilize template strings (`text ${var}`) para combiná-las em uma única frase.

let numero = 19;
let texto = "anos";
let bool = true;

console.log(`Eu tenho ${numero} ${texto}? ${bool}`);